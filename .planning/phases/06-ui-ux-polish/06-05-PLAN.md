---
phase: 06-ui-ux-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/middleware.ts
  - src/app/[locale]/(app)/settings/page.tsx
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Every page uses the dark theme with a single sidebar and single header — no layout collision"
    - "User can toggle the language between EN and JP and ALL UI text switches, including the Settings page heading"
    - "Sidebar navigation renders once with 4 routes and active state detection"
    - "Visiting / redirects to /en/ via next-intl middleware"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Clean root layout with ThemeProvider + Toaster only, no old sidebar/header"
      contains: "ThemeProvider"
    - path: "src/middleware.ts"
      provides: "next-intl locale routing middleware"
      contains: "createMiddleware"
    - path: "src/app/[locale]/(app)/settings/page.tsx"
      provides: "Settings page with translated title"
      contains: "getTranslations"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers/theme-provider.tsx"
      via: "ThemeProvider wraps children without old SidebarProvider"
      pattern: "<ThemeProvider>[^]*children[^]*</ThemeProvider>"
    - from: "src/middleware.ts"
      to: "src/i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "createMiddleware.*routing"
---

<objective>
Fix the root layout collision that blocks all 4 Phase 6 verification truths. The root layout still renders pre-Phase 6 SidebarProvider, AppSidebar, and AppHeader alongside the new dashboard shell. This single cleanup, plus adding middleware.ts and fixing the settings page heading, resolves all 4 gaps.

Purpose: Unblock dark theme, sidebar, locale toggle, and tablet verification.
Output: Clean root layout, working locale middleware, fully translated settings page.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-ux-polish/06-VERIFICATION.md
@src/app/layout.tsx
@src/app/[locale]/(app)/layout.tsx
@src/i18n/routing.ts
@src/app/[locale]/(app)/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip old layout components and add locale middleware</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/middleware.ts</files>
  <action>
    **src/app/layout.tsx:**
    1. Remove imports: `SidebarProvider` from `@/components/ui/sidebar`, `AppSidebar` from `@/components/app-sidebar`, `AppHeader` from `@/components/app-header` (lines 5-7)
    2. Remove `className="dark"` from the `<html>` tag (line 32) — keep `lang="en"` and `suppressHydrationWarning`
    3. Replace the entire body JSX with just ThemeProvider wrapping {children} and Toaster:
       ```
       <ThemeProvider>
         {children}
       </ThemeProvider>
       <Toaster />
       ```
       Remove the SidebarProvider, AppSidebar, AppHeader, and their wrapping divs (lines 37-45). Keep fonts, metadata, ThemeProvider, and Toaster.

    **src/middleware.ts** (new file):
    Create next-intl middleware using createMiddleware from `next-intl/middleware` and the `routing` config from `@/i18n/routing`. Export the middleware as default. Add a `config` export with `matcher` that excludes `_next`, static files, and API routes:
    ```ts
    import createMiddleware from 'next-intl/middleware'
    import { routing } from '@/i18n/routing'

    export default createMiddleware(routing)

    export const config = {
      matcher: ['/', '/(en|ja)/:path*']
    }
    ```

    **src/app/page.tsx:**
    Replace the placeholder content with a redirect to the default locale. Use `redirect` from `next/navigation` to send `/` to `/en/`:
    ```ts
    import { redirect } from 'next/navigation'
    export default function Home() {
      redirect('/en/')
    }
    ```
  </action>
  <verify>
    1. `npx tsc --noEmit` passes — no type errors from removed imports
    2. `grep -c "AppSidebar\|AppHeader\|SidebarProvider" src/app/layout.tsx` returns 0
    3. `test -f src/middleware.ts` — middleware file exists
    4. `grep "createMiddleware" src/middleware.ts` — middleware uses next-intl
  </verify>
  <done>Root layout renders only ThemeProvider + Toaster (no old sidebar/header). Middleware handles locale routing. Root page redirects to /en/.</done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded Settings heading to use translations</name>
  <files>src/app/[locale]/(app)/settings/page.tsx</files>
  <action>
    1. Import `getTranslations` from `next-intl/server`
    2. Make the function async (it already is)
    3. Add `const t = await getTranslations('settings')` at the top of the function body (after the existing Promise.all)
    4. Replace `<h1 className="text-2xl font-semibold tracking-tight">Settings</h1>` with `<h1 className="text-2xl font-semibold tracking-tight">{t('title')}</h1>`
    5. Verify that `messages/en.json` has `settings.title` = "Settings" and `messages/ja.json` has `settings.title` = "設定" (these should already exist from 06-03)
  </action>
  <verify>
    1. `grep "getTranslations" src/app/\\[locale\\]/\\(app\\)/settings/page.tsx` — import present
    2. `grep "t('title')" src/app/\\[locale\\]/\\(app\\)/settings/page.tsx` — translated heading used
    3. `grep -c "Settings</h1>" src/app/\\[locale\\]/\\(app\\)/settings/page.tsx` returns 0 — no hardcoded heading
  </verify>
  <done>Settings page heading is fully translated via t('title') — switches with locale toggle.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all 4 Phase 6 truths</name>
  <files>none</files>
  <action>
    Human verification of all 4 Phase 6 truths after gap closure.
    Run `npm run dev` and verify:
    1. Open http://localhost:3000/ — should redirect to /en/
    2. Navigate to /en/customers — verify: single dark outer shell (#2a2a2a), single 90px sidebar on left with icons, rounded content area, NO second sidebar or app header
    3. Click the locale toggle in TopBar — verify all UI text switches between EN and JP (check customers page, settings page heading)
    4. Click the theme toggle — verify smooth dark/light switch with no flash
    5. Navigate via sidebar: Recording, Customers, Karute, Settings — verify active state highlights correctly
    6. Resize browser to 1024px tablet width — verify no broken layouts, single sidebar, content fills remaining space
  </action>
  <verify>Human observes all 6 checks above pass visually in the browser.</verify>
  <done>All 4 Phase 6 truths verified by human: dark theme consistent, locale toggle works everywhere, sidebar renders once, tablet layout is usable.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. `npm run build` — builds successfully
3. No references to AppSidebar, AppHeader, or SidebarProvider in src/app/layout.tsx
4. middleware.ts exists and exports createMiddleware
5. Settings page uses getTranslations, not hardcoded text
6. Human verification of visual consistency, locale toggle, sidebar, and tablet layout
</verification>

<success_criteria>
All 4 Phase 6 verification truths pass:
1. Dark theme with single sidebar/header (no layout collision)
2. Locale toggle works on ALL pages including Settings
3. Sidebar renders once with 4 routes and active state
4. Tablet-friendly layout at 1024px
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-polish/06-05-SUMMARY.md`
</output>
