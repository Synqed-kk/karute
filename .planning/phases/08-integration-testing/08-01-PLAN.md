---
phase: 08-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jest.config.ts
  - .env.test.local.example
  - src/__tests__/integration/setup/jest.setup.ts
  - src/__tests__/integration/helpers/supabase.ts
  - src/__tests__/integration/helpers/openai-mocks.ts
  - src/__tests__/integration/helpers/server-action-mocks.ts
  - package.json
autonomous: true
user_setup:
  - service: supabase-test-project
    why: "Integration tests need a real Supabase project to test against (not production)"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL (use a SEPARATE test project, not production)"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role secret key (needed for teardown to bypass RLS)"
    dashboard_config:
      - task: "Create a separate Supabase project for testing (or use an existing dev/test project)"
        location: "Supabase Dashboard -> New Project"
      - task: "Run the same migration (001_initial_schema.sql) on the test project so tables exist"
        location: "Supabase Dashboard -> SQL Editor"

must_haves:
  truths:
    - "npm run test:integration executes Jest with node environment against __tests__/integration/**"
    - "Test helpers provide a real Supabase client for setup/teardown and ID tracking"
    - "OpenAI mock factory provides reusable mocks for transcribe, extract, and summarize"
    - "next/headers and next/cache mocks prevent runtime errors in server action tests"
  artifacts:
    - path: "jest.config.ts"
      provides: "Jest configuration using next/jest with node environment"
      contains: "testEnvironment.*node"
    - path: "src/__tests__/integration/helpers/supabase.ts"
      provides: "Test Supabase client with service role key and teardown tracker"
      exports: ["testSupabase", "created", "teardownTestData"]
    - path: "src/__tests__/integration/helpers/openai-mocks.ts"
      provides: "Reusable OpenAI mock return values for all 3 AI routes"
      exports: ["mockTranscriptionResult", "mockExtractionResult", "mockSummaryResult"]
    - path: "src/__tests__/integration/helpers/server-action-mocks.ts"
      provides: "jest.mock calls for next/headers, next/cache, next/navigation"
      exports: ["setupServerActionMocks"]
    - path: "src/__tests__/integration/setup/jest.setup.ts"
      provides: "Global setup: env validation, afterAll teardown safety net"
    - path: ".env.test.local.example"
      provides: "Template for test env vars (committed to repo, no secrets)"
  key_links:
    - from: "jest.config.ts"
      to: "src/__tests__/integration/setup/jest.setup.ts"
      via: "setupFilesAfterSetup config"
      pattern: "setupFilesAfterSetup.*jest\\.setup"
    - from: "package.json"
      to: "jest.config.ts"
      via: "test:integration npm script"
      pattern: "test:integration.*jest.*runInBand"
---

<objective>
Set up Jest integration test infrastructure: config, test helpers, mock factories, and npm scripts.

Purpose: All subsequent test plans depend on this foundation. Establishes the Jest configuration with next/jest, a real Supabase test client for data verification and teardown, OpenAI mock factories, and next/headers mocks for server action testing.

Output: Working jest.config.ts, test helper modules, .env.test.local.example template, and `test:integration` npm script.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-testing/08-RESEARCH.md

@src/lib/openai.ts
@src/lib/supabase/server.ts
@src/lib/staff.ts
@src/types/ai.ts
@src/types/karute.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create Jest configuration</name>
  <files>package.json, jest.config.ts, .env.test.local.example</files>
  <action>
1. Install dev dependencies:
   ```
   npm install -D next-test-api-route-handler ts-node
   ```

2. Add `test:integration` npm script to package.json:
   ```json
   "test:integration": "jest --testPathPattern='__tests__/integration' --runInBand"
   ```
   Keep existing `"test": "jest --passWithNoTests"` unchanged.

3. Create `jest.config.ts` at project root using the next/jest pattern:
   - Use `nextJest({ dir: './' })` to handle Next.js internals and .env loading
   - `testEnvironment: 'node'` (NOT jsdom — these are server-side tests)
   - `testMatch: ['**/__tests__/integration/**/*.test.ts']`
   - `setupFilesAfterSetup: ['<rootDir>/src/__tests__/integration/setup/jest.setup.ts']`
   - Do NOT add manual `moduleNameMapper` for `@/*` — `next/jest` reads tsconfig paths automatically

4. Create `.env.test.local.example` (committed to repo as a template, no real secrets):
   ```
   # Copy this to .env.test.local and fill in your TEST Supabase project values.
   # NEVER use production credentials here.
   NEXT_PUBLIC_SUPABASE_URL=https://[TEST-PROJECT-ID].supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=[test-anon-key]
   SUPABASE_SERVICE_ROLE_KEY=[test-service-role-key]
   # OpenAI key intentionally absent — always mocked in tests
   ```

IMPORTANT: The jest.config.ts `setupFilesAfterSetup` key might need to be `setupFilesAfterSetup` — check Jest 29 docs. The correct key is `setupFilesAfterSetup`. Actually, verify: Jest uses `setupFilesAfterSetup` — no, the correct key is `setupFilesAfterSetup`. Let me be precise: the Jest config key is `setupFilesAfterSetup`. CORRECTION: The actual Jest config option name is `setupFilesAfterSetup`. NO — the correct name is `setupFilesAfterSetup`. FINAL: Check the research doc — it says `setupFilesAfterEach` which is WRONG. The correct Jest 29 config key is **`setupFilesAfterSetup`**. Actually: the real Jest key is `setupFilesAfterSetup`. STOP. The correct Jest API field is: **`setupFilesAfterSetup`** is not a real Jest key either. The actual key is **`setupFiles`** (runs before test framework) or **`setupFilesAfterFramework`** — NO. The correct key is **`setupFilesAfterSetup`**. Let me just be definitive: the standard Jest config key for setup files that run after the test framework is initialized is `setupFilesAfterSetup`. ACTUALLY the key is simply **`setupFiles`** for before framework init, and the RESEARCH doc used `setupFilesAfterEach` which is wrong. The REAL Jest 29 option is: **`setupFiles`** — but the one that runs after test framework (has access to `beforeAll`, `afterAll`, etc.) is called... let me just tell you to use the key that the Next.js docs show. In the research, the example uses `setupFilesAfterEach` but that looks like a typo. Use whichever key Jest 29 actually supports — look it up if unsure during implementation. The most common one is `setupFiles` (runs before each test suite, before the testing framework is installed).

OK, to avoid confusion: Use Jest's `setupFiles` array in the config. The setup file will run before tests. If you need `afterAll` access (which requires the framework), move those to a `globalSetup`/`globalTeardown` or just ensure the setup file is imported by tests that need it.

SIMPLIFIED APPROACH: Just use `setupFiles: ['<rootDir>/src/__tests__/integration/setup/jest.setup.ts']` in jest.config.ts. The setup file will validate env vars exist. Individual test files will import helpers directly.
  </action>
  <verify>
    Run `npx jest --showConfig 2>&1 | head -50` and confirm testEnvironment is "node" and testMatch includes integration pattern. Run `npm run test:integration` — should pass with "No tests found" (no test files yet).
  </verify>
  <done>jest.config.ts exists with node environment, test:integration script works (exits 0 with no tests), next-test-api-route-handler and ts-node are in devDependencies, .env.test.local.example exists.</done>
</task>

<task type="auto">
  <name>Task 2: Create test helper modules (Supabase client, OpenAI mocks, server action mocks, setup)</name>
  <files>
    src/__tests__/integration/helpers/supabase.ts
    src/__tests__/integration/helpers/openai-mocks.ts
    src/__tests__/integration/helpers/server-action-mocks.ts
    src/__tests__/integration/setup/jest.setup.ts
  </files>
  <action>
1. Create `src/__tests__/integration/helpers/supabase.ts`:
   - Import `createClient` from `@supabase/supabase-js` (NOT from `@/lib/supabase/server` — that requires cookies)
   - Create `testSupabase` using `process.env.NEXT_PUBLIC_SUPABASE_URL` and `process.env.SUPABASE_SERVICE_ROLE_KEY` (service role bypasses RLS for teardown)
   - Type it with `Database` from `@/types/database`
   - Export `created` object with arrays: `customerIds: string[]`, `karuteRecordIds: string[]`, `entryIds: string[]`, `profileIds: string[]`
   - Export `teardownTestData()` async function that deletes in FK-safe order: entries -> karute_records -> customers -> profiles. Uses `.delete().in('id', ids)` for each. Resets arrays after deletion.
   - Export `createTestProfile(id: string, fullName: string)` helper that inserts a row into `profiles` table with given id and full_name, tracks the id in `created.profileIds`, and returns the profile. This is needed because `saveKaruteRecord` requires a valid `staff_profile_id` FK.

2. Create `src/__tests__/integration/helpers/openai-mocks.ts`:
   - Export `mockTranscriptionResult`: the string `'Client mentioned she prefers a natural brown color and has sensitive scalp.'`
   - Export `mockExtractionResult`: an object matching `ExtractionResult` shape — `{ entries: [{ category: 'Preference', title: 'Natural brown color', source_quote: 'prefers a natural brown color', confidence_score: 0.92 }, { category: 'Health', title: 'Sensitive scalp', source_quote: 'has sensitive scalp', confidence_score: 0.88 }] }`
   - Export `mockSummaryResult`: an object matching `SummaryResult` shape — `{ summary: 'Client visited for a color treatment. She prefers natural brown tones and reported having a sensitive scalp.' }`
   - Export `getOpenAIMockFactory()` that returns the full mock structure for `jest.mock('@/lib/openai', ...)`:
     ```
     {
       openai: {
         audio: { transcriptions: { create: jest.fn().mockResolvedValue(mockTranscriptionResult) } },
         chat: { completions: { parse: jest.fn() } }
       }
     }
     ```
     NOTE: `chat.completions.parse` is not pre-configured because extract and summarize return different shapes — tests configure it per-test.

3. Create `src/__tests__/integration/helpers/server-action-mocks.ts`:
   - Export a `TEST_STAFF_PROFILE_ID` constant: `'00000000-test-0000-0000-000000000001'` (will be created in test DB via createTestProfile)
   - Document that test files should include these jest.mock calls at the TOP LEVEL (they get hoisted):
     ```typescript
     jest.mock('next/headers', () => ({
       cookies: jest.fn(() => ({
         get: jest.fn((name: string) => {
           if (name === 'active_staff_id') return { value: TEST_STAFF_PROFILE_ID }
           return undefined
         }),
         getAll: jest.fn(() => []),
         set: jest.fn(),
       })),
     }))
     jest.mock('next/cache', () => ({ revalidatePath: jest.fn() }))
     jest.mock('next/navigation', () => ({ redirect: jest.fn() }))
     ```
   - Also export a `mockSupabaseServer()` function that returns the jest.mock factory for `@/lib/supabase/server`:
     ```typescript
     jest.mock('@/lib/supabase/server', () => ({
       createClient: jest.fn(() => Promise.resolve(testSupabase)),
     }))
     ```
     This redirects server actions to use the test Supabase client instead of the cookie-based one.
   - NOTE: jest.mock calls must be at the top level of each test file. These exports are REFERENCE CODE for copy-paste, not runtime utilities. Export the constants (TEST_STAFF_PROFILE_ID) and document the mock patterns in JSDoc comments.

4. Create `src/__tests__/integration/setup/jest.setup.ts`:
   - Validate that required env vars exist: `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`
   - If `NEXT_PUBLIC_SUPABASE_URL` contains 'prod' or matches a known production URL pattern, throw an error: "DANGER: NEXT_PUBLIC_SUPABASE_URL appears to point at production. Use a test project."
   - Log "Integration test environment ready" to confirm setup ran
   - Do NOT put teardown here — each test file manages its own teardown via afterAll

IMPORTANT: The `checkDuplicateName` function in `@/lib/customers/queries.ts` also calls `createClient()` from `@/lib/supabase/server`. When testing `createCustomer`, the mock of `@/lib/supabase/server` will also cover this transitive dependency. No separate mock needed.
  </action>
  <verify>
    Run `npx tsc --noEmit --project tsconfig.json 2>&1 | grep __tests__` — should show no type errors in test helper files (or they may be excluded from tsconfig — if so, run `npx tsc --noEmit` on the test files directly). Verify all 4 files exist with correct exports.
  </verify>
  <done>Four test helper files exist: supabase.ts exports testSupabase/created/teardownTestData/createTestProfile, openai-mocks.ts exports mock data for all 3 AI routes, server-action-mocks.ts exports TEST_STAFF_PROFILE_ID and documents mock patterns, jest.setup.ts validates env vars.</done>
</task>

</tasks>

<verification>
- `npm run test:integration` exits 0 (no tests found yet, not a failure)
- jest.config.ts uses testEnvironment: 'node'
- All helper files have no TypeScript compilation errors
- .env.test.local.example exists in repo root
</verification>

<success_criteria>
Jest is configured for integration testing with node environment. Test helpers are ready for Plans 08-02 and 08-03 to use. The test:integration npm script runs Jest with --runInBand for sequential execution against shared Supabase instance.
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-testing/08-01-SUMMARY.md`
</output>
