---
phase: 01-foundation-recording
plan: "06"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/hooks/use-media-recorder.ts
  - src/hooks/use-waveform-bars.ts
  - src/hooks/use-recording-timer.ts
autonomous: true

must_haves:
  truths:
    - "useMediaRecorder hook starts browser mic recording and produces an audio Blob on stop"
    - "useMediaRecorder supports pause/resume — state machine: idle → recording → paused ↔ recording → stopped"
    - "MediaRecorder format is negotiated at runtime via isTypeSupported() — not hardcoded"
    - "useWaveformBars hook returns number[30] bar heights computed from AnalyserNode RMS + cosine envelope"
    - "useRecordingTimer hook tracks elapsed seconds, freezes when paused, and formats as MM:SS"
    - "AudioContext is created inside the user gesture handler — not at module load time"
  artifacts:
    - path: "src/hooks/use-media-recorder.ts"
      provides: "MediaRecorder lifecycle hook with cross-browser format negotiation and pause/resume"
      contains: "pauseRecording"
    - path: "src/hooks/use-waveform-bars.ts"
      provides: "30-bar CSS pill waveform hook returning number[] heights from AnalyserNode RMS"
      contains: "cosineEnvelope"
    - path: "src/hooks/use-recording-timer.ts"
      provides: "Elapsed time counter hook returning formatted MM:SS string, freezes on pause"
      contains: "setInterval"
  key_links:
    - from: "src/hooks/use-media-recorder.ts"
      to: "navigator.mediaDevices.getUserMedia"
      via: "requestMicAccess inside startRecording()"
      pattern: "getUserMedia"
    - from: "src/hooks/use-waveform-bars.ts"
      to: "AudioContext"
      via: "new AudioContext() inside useEffect when stream is non-null"
      pattern: "new AudioContext"
    - from: "src/hooks/use-waveform-bars.ts"
      to: "AnalyserNode RMS"
      via: "getByteTimeDomainData → RMS amplitude per bar bucket"
      pattern: "getByteTimeDomainData"
---

<objective>
Build the browser mic recording hooks: MediaRecorder with cross-browser format negotiation and full pause/resume support, 30-bar CSS pill waveform visualization via Web Audio API, and an elapsed timer that freezes on pause.

Purpose: The recording panel UI (01-07) depends on these hooks. Running Wave 2 parallel with 01-02, 01-04, 01-05 — only needs the Next.js scaffold from 01-01.
Output: Three reusable hooks ready for the recording panel to import. No canvas — waveform is 30 div bars driven by number[] heights from this hook.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-recording/01-RESEARCH.md
@.planning/phases/01-foundation-recording/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useMediaRecorder hook with format negotiation and pause/resume</name>
  <files>
    src/hooks/use-media-recorder.ts
  </files>
  <action>
    Create `src/hooks/use-media-recorder.ts`:

    The state machine is: idle → recording → paused ↔ recording → stopped (recorded)
    NOTE: 'paused' is a distinct state — the timer freezes and waveform goes flat when paused.

    ```typescript
    'use client'
    import { useState, useRef, useCallback } from 'react'

    // Full state machine: idle → recording → paused ↔ recording → recorded
    export type RecordingState = 'idle' | 'recording' | 'paused' | 'recorded'

    export interface RecordingResult {
      blob: Blob
      mimeType: string
      durationMs: number
    }

    /**
     * Negotiate the best supported audio format at runtime.
     * Priority: webm/opus (best compression) → webm → mp4 (iOS Safari) → ogg → wav (fallback)
     * NEVER hardcode a format — iOS Safari < 18.4 does not support audio/webm.
     */
    function getSupportedMimeType(): string {
      const formats = [
        'audio/webm;codecs=opus',
        'audio/webm',
        'audio/mp4',
        'audio/ogg;codecs=opus',
        'audio/wav',
      ]
      return formats.find(f => MediaRecorder.isTypeSupported(f)) ?? ''
    }

    export function useMediaRecorder() {
      const [state, setState] = useState<RecordingState>('idle')
      const [result, setResult] = useState<RecordingResult | null>(null)
      const [error, setError] = useState<string | null>(null)
      const [stream, setStream] = useState<MediaStream | null>(null)

      const mediaRecorderRef = useRef<MediaRecorder | null>(null)
      const chunksRef = useRef<Blob[]>([])
      const startTimeRef = useRef<number>(0)
      const pausedDurationRef = useRef<number>(0) // Accumulates time paused
      const pauseStartRef = useRef<number>(0)     // When current pause began

      const startRecording = useCallback(async () => {
        setError(null)
        setResult(null)
        chunksRef.current = []
        pausedDurationRef.current = 0

        let micStream: MediaStream
        try {
          micStream = await navigator.mediaDevices.getUserMedia({ audio: true, video: false })
        } catch (err) {
          setError('Microphone access denied. Please allow microphone access and try again.')
          return
        }

        setStream(micStream)

        const mimeType = getSupportedMimeType()
        const recorder = new MediaRecorder(
          micStream,
          mimeType ? { mimeType } : undefined
        )

        recorder.ondataavailable = (e) => {
          if (e.data.size > 0) chunksRef.current.push(e.data)
        }

        recorder.onstop = () => {
          const totalElapsed = Date.now() - startTimeRef.current
          const durationMs = totalElapsed - pausedDurationRef.current
          const blob = new Blob(chunksRef.current, {
            type: mimeType || recorder.mimeType,
          })
          setResult({ blob, mimeType: mimeType || recorder.mimeType, durationMs })
          setState('recorded')

          // Stop all mic tracks to release the microphone indicator
          micStream.getTracks().forEach(track => track.stop())
          setStream(null)
        }

        mediaRecorderRef.current = recorder
        startTimeRef.current = Date.now()
        recorder.start(100) // Collect chunks every 100ms for smooth waveform data
        setState('recording')
      }, [])

      const stopRecording = useCallback(() => {
        if (mediaRecorderRef.current && mediaRecorderRef.current.state !== 'inactive') {
          // Resume before stopping if paused — MediaRecorder.stop() requires active state
          if (mediaRecorderRef.current.state === 'paused') {
            mediaRecorderRef.current.resume()
          }
          mediaRecorderRef.current.stop()
        }
      }, [])

      const pauseRecording = useCallback(() => {
        if (mediaRecorderRef.current && mediaRecorderRef.current.state === 'recording') {
          mediaRecorderRef.current.pause()
          pauseStartRef.current = Date.now()
          setState('paused')
        }
      }, [])

      const resumeRecording = useCallback(() => {
        if (mediaRecorderRef.current && mediaRecorderRef.current.state === 'paused') {
          mediaRecorderRef.current.resume()
          // Accumulate time spent paused so durationMs excludes it
          pausedDurationRef.current += Date.now() - pauseStartRef.current
          setState('recording')
        }
      }, [])

      const discardRecording = useCallback(() => {
        setResult(null)
        setError(null)
        chunksRef.current = []
        pausedDurationRef.current = 0
        // Ensure stream tracks are stopped if somehow still active
        stream?.getTracks().forEach(track => track.stop())
        setStream(null)
        setState('idle')
      }, [stream])

      return {
        state,
        result,
        error,
        stream,
        startRecording,
        stopRecording,
        pauseRecording,
        resumeRecording,
        discardRecording,
      }
    }
    ```
  </action>
  <verify>
    Run: `npm run type-check` — exits 0.
    Check: `grep "isTypeSupported" src/hooks/use-media-recorder.ts` — found.
    Check: `grep "getUserMedia" src/hooks/use-media-recorder.ts` — found.
    Check: `grep "audio/mp4" src/hooks/use-media-recorder.ts` — found (iOS Safari format in list).
    Check: `grep "paused" src/hooks/use-media-recorder.ts` — found in RecordingState type.
    Check: `grep "pauseRecording\|resumeRecording" src/hooks/use-media-recorder.ts` — both found.
  </verify>
  <done>
    useMediaRecorder exports startRecording, stopRecording, pauseRecording, resumeRecording, discardRecording, state (idle/recording/paused/recorded), result, stream. RecordingState includes 'paused'. Format negotiated via isTypeSupported. TypeScript passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: useWaveformBars (30 CSS pill bars) and useRecordingTimer hooks</name>
  <files>
    src/hooks/use-waveform-bars.ts
    src/hooks/use-recording-timer.ts
  </files>
  <action>
    Create `src/hooks/use-waveform-bars.ts`:

    This hook returns `number[30]` bar heights (0–100) computed from AnalyserNode RMS amplitude
    with a cosine envelope (center bars tallest, edges shortest). No canvas — the UI renders
    30 div elements styled as pill bars using these heights.

    When `active` is false (paused or idle), all bars return to 8px flat (heights all return 8).

    ```typescript
    'use client'
    import { useEffect, useRef, useState } from 'react'

    const BAR_COUNT = 30

    /**
     * Cosine envelope: center bars are tallest, edges are shortest.
     * Returns a multiplier 0.15–1.0 for each bar position.
     */
    function cosineEnvelope(index: number, total: number): number {
      return 0.15 + 0.85 * Math.cos(((index / (total - 1)) - 0.5) * Math.PI) ** 2
    }

    /**
     * Compute RMS amplitude from a time-domain data buffer slice.
     * Returns a value 0.0–1.0.
     */
    function rms(data: Uint8Array, start: number, end: number): number {
      let sum = 0
      const count = end - start
      for (let i = start; i < end; i++) {
        const normalized = (data[i] - 128) / 128 // -1.0 to 1.0
        sum += normalized * normalized
      }
      return Math.sqrt(sum / count)
    }

    /**
     * useWaveformBars — returns number[30] bar heights (8–100px range).
     *
     * @param stream  Live MediaStream from getUserMedia. null = bars go flat.
     * @param active  When false (paused state), bars return to flat 8px immediately.
     *
     * CRITICAL: AudioContext must be created INSIDE useEffect (triggered by stream change),
     * not at module load time. Browsers suspend AudioContext created without user gesture.
     * The stream is provided only after the user clicks — that IS the gesture.
     */
    export function useWaveformBars(
      stream: MediaStream | null,
      active: boolean
    ): number[] {
      const [bars, setBars] = useState<number[]>(Array(BAR_COUNT).fill(8))
      const animFrameRef = useRef<number>()
      const audioCtxRef = useRef<AudioContext>()

      useEffect(() => {
        if (!stream || !active) {
          // Bars go flat when not recording (idle or paused)
          if (animFrameRef.current) cancelAnimationFrame(animFrameRef.current)
          setBars(Array(BAR_COUNT).fill(8))
          return
        }

        // Create AudioContext inside the effect — stream only exists after user gesture
        const audioCtx = new AudioContext()
        audioCtxRef.current = audioCtx

        if (audioCtx.state === 'suspended') {
          audioCtx.resume()
        }

        const analyser = audioCtx.createAnalyser()
        analyser.fftSize = 2048
        analyser.smoothingTimeConstant = 0.8

        const source = audioCtx.createMediaStreamSource(stream)
        source.connect(analyser)

        const bufferLength = analyser.frequencyBinCount
        const dataArray = new Uint8Array(bufferLength)
        const bucketSize = Math.floor(bufferLength / BAR_COUNT)

        function draw() {
          animFrameRef.current = requestAnimationFrame(draw)
          analyser.getByteTimeDomainData(dataArray)

          const newBars = Array.from({ length: BAR_COUNT }, (_, i) => {
            const start = i * bucketSize
            const end = start + bucketSize
            const amplitude = rms(dataArray, start, end)
            const envelope = cosineEnvelope(i, BAR_COUNT)
            // Scale: 8px flat minimum, up to 100px at full amplitude
            // Add small organic jitter (+/- 2px) for natural look
            const height = 8 + amplitude * envelope * 92 + (Math.random() - 0.5) * 4
            return Math.max(8, Math.min(100, height))
          })

          setBars(newBars)
        }

        draw()

        return () => {
          if (animFrameRef.current) cancelAnimationFrame(animFrameRef.current)
          audioCtx.close()
        }
      }, [stream, active])

      return bars
    }
    ```

    Create `src/hooks/use-recording-timer.ts`:
    ```typescript
    'use client'
    import { useState, useEffect, useRef } from 'react'

    /**
     * Tracks elapsed recording time in seconds.
     * Returns a formatted MM:SS string and the raw elapsed seconds.
     * Timer runs when `isRunning` is true, FREEZES (does not reset) when false.
     * Resets to 0 when isRunning transitions from false → true (new recording start).
     */
    export function useRecordingTimer(isRunning: boolean) {
      const [elapsedSeconds, setElapsedSeconds] = useState(0)
      const intervalRef = useRef<ReturnType<typeof setInterval>>()
      const wasRunningRef = useRef(false)

      useEffect(() => {
        if (isRunning) {
          // Reset if starting fresh (was not running before)
          if (!wasRunningRef.current) {
            setElapsedSeconds(0)
          }
          wasRunningRef.current = true
          intervalRef.current = setInterval(() => {
            setElapsedSeconds(s => s + 1)
          }, 1000)
        } else {
          // Freeze (do not reset) — paused state keeps the timer value
          if (intervalRef.current) clearInterval(intervalRef.current)
          // Only clear wasRunning when explicitly reset (discard)
        }
        return () => {
          if (intervalRef.current) clearInterval(intervalRef.current)
        }
      }, [isRunning])

      const formatted = formatTime(elapsedSeconds)

      return { elapsedSeconds, formatted }
    }

    function formatTime(seconds: number): string {
      const m = Math.floor(seconds / 60)
      const s = seconds % 60
      return `${String(m).padStart(2, '0')}:${String(s).padStart(2, '0')}`
    }
    ```
  </action>
  <verify>
    Run: `npm run type-check` — exits 0.
    Check: `grep "new AudioContext()" src/hooks/use-waveform-bars.ts` — found inside useEffect (not at module level).
    Check: `grep "cosineEnvelope" src/hooks/use-waveform-bars.ts` — found.
    Check: `grep "getByteTimeDomainData" src/hooks/use-waveform-bars.ts` — found.
    Check: `grep "BAR_COUNT = 30" src/hooks/use-waveform-bars.ts` — found.
    Check: `grep "setInterval" src/hooks/use-recording-timer.ts` — found.
    Check: `grep "formatTime" src/hooks/use-recording-timer.ts` — found with MM:SS formatting.
    Check: File is named `use-waveform-bars.ts` NOT `use-waveform.ts` (no canvas hook).
  </verify>
  <done>
    useWaveformBars returns number[30] bar heights driven by AnalyserNode RMS + cosine envelope. AudioContext created inside useEffect. Active=false → all bars return 8 (flat/frozen). useRecordingTimer returns { elapsedSeconds, formatted } with MM:SS format, freezes (not resets) when isRunning=false. TypeScript passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run type-check` exits 0
2. `grep "isTypeSupported" src/hooks/use-media-recorder.ts` — found
3. `grep "audio/mp4" src/hooks/use-media-recorder.ts` — found (iOS Safari format)
4. `grep "paused" src/hooks/use-media-recorder.ts` — in RecordingState type
5. `grep "pauseRecording\|resumeRecording" src/hooks/use-media-recorder.ts` — both found
6. AudioContext in useWaveformBars created inside useEffect, not at module scope
7. `grep "cosineEnvelope\|BAR_COUNT = 30" src/hooks/use-waveform-bars.ts` — both found
8. No `use-waveform.ts` canvas hook created (bars-only approach)
9. No `any` types in hook implementations
</verification>

<success_criteria>
- useMediaRecorder: startRecording, stopRecording, pauseRecording, resumeRecording, discardRecording, state (idle/recording/paused/recorded), result (blob + mimeType), stream
- RecordingState includes 'paused' — MediaRecorder.pause()/resume() called, state transitions correctly
- Format negotiation via isTypeSupported() — formats tried: webm;codecs=opus, webm, mp4, ogg, wav
- useWaveformBars: returns number[30] heights (8–100), cosine envelope, RMS from AnalyserNode, flat at 8 when active=false
- useRecordingTimer: counts seconds when isRunning=true, FREEZES (does not reset) when false, returns MM:SS formatted string
- TypeScript strict mode passes across all three hook files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-recording/01-06-SUMMARY.md`
</output>
