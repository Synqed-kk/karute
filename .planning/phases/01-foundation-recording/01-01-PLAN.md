---
phase: 01-foundation-recording
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/app-sidebar.tsx
  - src/components/app-header.tsx
  - src/components/ui/sidebar.tsx
  - next.config.ts
  - tsconfig.json
  - .env.local.example
autonomous: true

must_haves:
  truths:
    - "Developer runs `npm run dev` and sees the app at localhost:3000 without errors"
    - "App shell renders with dark background and a collapsible sidebar"
    - "Header renders with sun icon theme toggle, EN/JP language toggle, and user avatar + name dropdown"
    - "TypeScript compilation (`tsc --noEmit`) exits with 0 errors"
    - "Tailwind v4 CSS-first config is in globals.css — no tailwind.config.js exists"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @import, dark mode @custom-variant, shadcn CSS variable tokens for dark theme"
      contains: "@import \"tailwindcss\""
    - path: "src/components/app-sidebar.tsx"
      provides: "AppSidebar component using shadcn Sidebar primitive"
      contains: "AppSidebar"
    - path: "src/components/app-header.tsx"
      provides: "AppHeader with theme toggle (sun icon), EN/JP toggle, avatar + name dropdown"
      contains: "AppHeader"
    - path: "src/app/layout.tsx"
      provides: "Root layout with dark class, SidebarProvider wrapping"
      contains: "SidebarProvider"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/app-sidebar.tsx"
      via: "AppSidebar rendered in root layout"
      pattern: "AppSidebar"
    - from: "src/app/layout.tsx"
      to: "src/components/app-header.tsx"
      via: "AppHeader rendered in root layout"
      pattern: "AppHeader"
    - from: "src/app/globals.css"
      to: "Tailwind v4"
      via: "@import tailwindcss"
      pattern: "@import \"tailwindcss\""
---

<objective>
Scaffold the Next.js 16 project with TypeScript, Tailwind v4 (CSS-first), shadcn components, a dark theme shell, the sidebar navigation structure, and the app header.

Purpose: Every other plan in Phase 1 depends on a working app scaffold. This is Wave 1 — must complete before anything else.
Output: Running Next.js 16 app at localhost:3000 with dark theme shell, collapsible sidebar, and app header with theme toggle, language toggle, and avatar dropdown.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-recording/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with all dependencies</name>
  <files>
    package.json
    next.config.ts
    tsconfig.json
    src/app/globals.css
    .env.local.example
  </files>
  <action>
    The working directory is /Users/anthonylee/karuteround2 — the project will be scaffolded here (not in a subdirectory).

    Run the scaffold command:
    ```
    npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
    ```

    After scaffold completes, install additional dependencies:
    ```
    npm install @supabase/supabase-js @supabase/ssr next-intl next-themes tw-animate-css zod sonner
    ```

    Then add shadcn:
    ```
    npx shadcn@latest init -t next --yes
    npx shadcn@latest add sidebar button dialog sheet avatar dropdown-menu --yes
    ```

    After shadcn init, verify and fix globals.css for Tailwind v4:
    - The file MUST start with `@import "tailwindcss";` and `@import "tw-animate-css";`
    - Add `@custom-variant dark (&:where(.dark, .dark *));` after the imports
    - shadcn may generate a `tailwind.config.js` — if it does, DELETE it (Tailwind v4 is CSS-first, no JS config)
    - Verify CSS variables for dark theme are in globals.css under `.dark { }` selector

    Create `.env.local.example` with:
    ```
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=
    ```
    (Use NEXT_PUBLIC_SUPABASE_ANON_KEY — not PUBLISHABLE_KEY — for compatibility per research open question #3)

    Verify `tsconfig.json` has `"moduleResolution": "bundler"` or `"node16"` (required for next-intl v4 ESM-only).

    In `package.json`, ensure scripts include:
    - `"lint": "eslint src"` — `next lint` was removed in Next.js 16
    - `"type-check": "tsc --noEmit"`
    - `"test": "jest --passWithNoTests"` (add jest as devDependency: `npm install -D jest @types/jest`)
  </action>
  <verify>
    Run: `npm run dev` — app starts on localhost:3000 without errors.
    Run: `npm run type-check` — exits 0.
    Check: `ls tailwind.config.js` — should NOT exist (file must be absent).
    Check: `head -3 src/app/globals.css` — must show `@import "tailwindcss"`.
  </verify>
  <done>
    App runs at localhost:3000, tsc passes, no tailwind.config.js exists, globals.css uses @import tailwindcss.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dark theme shell with collapsible sidebar and app header</name>
  <files>
    src/app/layout.tsx
    src/app/page.tsx
    src/components/app-sidebar.tsx
    src/components/app-header.tsx
  </files>
  <action>
    Create `src/components/app-sidebar.tsx` using the shadcn Sidebar primitive:
    ```tsx
    'use client'
    import {
      Sidebar, SidebarContent, SidebarGroup, SidebarGroupContent,
      SidebarHeader, SidebarFooter, SidebarMenu, SidebarMenuButton,
      SidebarMenuItem
    } from '@/components/ui/sidebar'

    const navItems = [
      { label: 'Recording', href: '/', icon: '🎙' },
      { label: 'Customers', href: '/customers', icon: '👥' },
      { label: 'Karute', href: '/karute', icon: '📋' },
      { label: 'Settings', href: '/settings', icon: '⚙️' },
    ]

    export function AppSidebar() {
      return (
        <Sidebar collapsible="icon">
          <SidebarHeader>
            <span className="font-bold text-lg px-2">Karute</span>
          </SidebarHeader>
          <SidebarContent>
            <SidebarGroup>
              <SidebarGroupContent>
                <SidebarMenu>
                  {navItems.map((item) => (
                    <SidebarMenuItem key={item.href}>
                      <SidebarMenuButton asChild>
                        <a href={item.href}>
                          <span>{item.icon}</span>
                          <span>{item.label}</span>
                        </a>
                      </SidebarMenuButton>
                    </SidebarMenuItem>
                  ))}
                </SidebarMenu>
              </SidebarGroupContent>
            </SidebarGroup>
          </SidebarContent>
          <SidebarFooter />
        </Sidebar>
      )
    }
    ```

    Create `src/components/app-header.tsx` (Client Component — needs state for dropdowns):
    ```tsx
    'use client'
    import { useState } from 'react'
    import { Sun, Globe } from 'lucide-react'
    import { Avatar, AvatarFallback } from '@/components/ui/avatar'
    import {
      DropdownMenu,
      DropdownMenuContent,
      DropdownMenuItem,
      DropdownMenuTrigger,
    } from '@/components/ui/dropdown-menu'
    import { Button } from '@/components/ui/button'
    import { SidebarTrigger } from '@/components/ui/sidebar'

    // Phase 1 placeholder staff — replaced with real auth user in Phase 3
    const PLACEHOLDER_STAFF = [
      { id: 'staff-1', name: 'Yuki Tanaka', initials: 'YT' },
      { id: 'staff-2', name: 'Hana Sato', initials: 'HS' },
      { id: 'staff-3', name: 'Kenji Mori', initials: 'KM' },
    ]

    export function AppHeader() {
      const [currentStaff, setCurrentStaff] = useState(PLACEHOLDER_STAFF[0])
      const [locale, setLocale] = useState<'EN' | 'JP'>('EN')

      function toggleTheme() {
        // Theme toggle — full next-themes integration added in 01-05 (i18n/theme plan)
        // For now, toggle the dark class on the html element
        document.documentElement.classList.toggle('dark')
      }

      return (
        <header className="flex items-center justify-between h-14 px-4 border-b border-border bg-background">
          <div className="flex items-center gap-2">
            <SidebarTrigger />
          </div>

          <div className="flex items-center gap-2">
            {/* Theme toggle — sun icon per mockup */}
            <Button
              variant="ghost"
              size="icon"
              onClick={toggleTheme}
              aria-label="Toggle theme"
            >
              <Sun className="h-5 w-5" />
            </Button>

            {/* EN/JP language toggle */}
            <Button
              variant="ghost"
              size="sm"
              onClick={() => setLocale(l => l === 'EN' ? 'JP' : 'EN')}
              className="font-medium text-sm min-w-[3rem]"
              aria-label="Toggle language"
            >
              <Globe className="h-4 w-4 mr-1" />
              {locale}
            </Button>

            {/* Staff avatar + name + dropdown — switching staff changes recording attribution */}
            <DropdownMenu>
              <DropdownMenuTrigger asChild>
                <Button variant="ghost" className="flex items-center gap-2 px-2">
                  <Avatar className="h-8 w-8">
                    <AvatarFallback className="bg-muted text-foreground text-xs">
                      {currentStaff.initials}
                    </AvatarFallback>
                  </Avatar>
                  <span className="text-sm font-medium hidden sm:inline">{currentStaff.name}</span>
                </Button>
              </DropdownMenuTrigger>
              <DropdownMenuContent align="end" className="w-48">
                {PLACEHOLDER_STAFF.map((staff) => (
                  <DropdownMenuItem
                    key={staff.id}
                    onClick={() => setCurrentStaff(staff)}
                    className={currentStaff.id === staff.id ? 'bg-accent' : ''}
                  >
                    <Avatar className="h-6 w-6 mr-2">
                      <AvatarFallback className="bg-muted text-foreground text-xs">
                        {staff.initials}
                      </AvatarFallback>
                    </Avatar>
                    {staff.name}
                  </DropdownMenuItem>
                ))}
              </DropdownMenuContent>
            </DropdownMenu>
          </div>
        </header>
      )
    }
    ```

    Update `src/app/layout.tsx` to:
    - Set `<html className="dark">` — dark class on html element activates dark theme
    - Wrap children in `SidebarProvider` from shadcn
    - Render `AppSidebar` and `AppHeader` alongside main content

    ```tsx
    import type { Metadata } from 'next'
    import { SidebarProvider } from '@/components/ui/sidebar'
    import { AppSidebar } from '@/components/app-sidebar'
    import { AppHeader } from '@/components/app-header'
    import './globals.css'

    export const metadata: Metadata = { title: 'Karute', description: 'Digital karute for service businesses' }

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="en" className="dark">
          <body>
            <SidebarProvider>
              <AppSidebar />
              <div className="flex flex-col flex-1 min-h-screen">
                <AppHeader />
                <main className="flex-1 bg-background text-foreground">
                  {children}
                </main>
              </div>
            </SidebarProvider>
          </body>
        </html>
      )
    }
    ```

    Update `src/app/page.tsx` to a minimal dark-themed placeholder:
    ```tsx
    export default function Home() {
      return (
        <div className="p-8">
          <h1 className="text-2xl font-bold">Karute</h1>
          <p className="text-muted-foreground mt-2">Recording dashboard — coming soon</p>
        </div>
      )
    }
    ```
  </action>
  <verify>
    Run: `npm run dev`
    Visit http://localhost:3000 — verify:
    1. Background is dark (near-black, not white)
    2. Sidebar appears on the left with nav items
    3. Header renders at top with: sun icon button, EN/JP toggle button, avatar with initials + name
    4. Clicking the avatar/name dropdown shows list of staff members
    5. Selecting a different staff member updates the avatar/name in the header
    6. SidebarTrigger in header collapses/expands the sidebar
    7. No hydration errors in browser console
    Run: `npm run type-check` — exits 0.
  </verify>
  <done>
    Dark theme shell renders at localhost:3000. Collapsible sidebar with Recording, Customers, Karute, Settings nav items. Header with sun icon, EN/JP toggle, and staff avatar dropdown — selecting staff member updates the displayed name. No TypeScript errors.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npm run type-check` exits 0
3. `npm run lint` exits 0
4. `ls tailwind.config.js` returns "No such file" — CSS-first config confirmed
5. `grep "@import \"tailwindcss\"" src/app/globals.css` — finds the import
6. `grep "dark" src/app/layout.tsx` — finds `className="dark"` on html element
7. `grep "AppHeader" src/app/layout.tsx` — found
8. `grep "Sun" src/components/app-header.tsx` — found (sun icon theme toggle per mockup)
9. Visiting localhost:3000 shows dark background with sidebar and header
</verification>

<success_criteria>
- Next.js 16 project runs at localhost:3000 with no errors
- Dark theme active by default (html.dark class)
- Collapsible sidebar navigation renders with 4 nav items
- Header renders with sun icon (theme toggle), EN/JP toggle, staff avatar + name + dropdown
- Staff avatar dropdown lets user switch active staff member (auto-generated initials badge)
- TypeScript strict mode passes
- No tailwind.config.js (Tailwind v4 CSS-first confirmed)
- All Phase 1 dependencies installed: @supabase/ssr, next-intl, next-themes, tw-animate-css, zod, sonner
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-recording/01-01-SUMMARY.md`
</output>
