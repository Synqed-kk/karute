---
phase: 04-karute-records
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/components/review/ReviewConfirmStep.tsx
  - src/app/(app)/review/page.tsx
  - src/app/(app)/karute/page.tsx
  - src/components/karute/KaruteListItem.tsx
autonomous: true

must_haves:
  truths:
    - "After confirming entries on the AI review screen, user sees the customer combobox and save button inline"
    - "Clicking save writes draft to sessionStorage, calls saveKaruteRecord, and redirects to /karute/[newId]"
    - "User can navigate to a karute list page and open any past karute record"
    - "The full flow works end-to-end: review screen → select customer → save → karute detail view"
  artifacts:
    - path: "src/components/review/ReviewConfirmStep.tsx"
      provides: "Confirm step that writes draft to sessionStorage and renders SaveKaruteFlow"
      min_lines: 30
    - path: "src/app/(app)/karute/page.tsx"
      provides: "Karute list page showing all saved records"
      min_lines: 20
    - path: "src/components/karute/KaruteListItem.tsx"
      provides: "List item for karute records with customer name, date, entry count"
      min_lines: 15
  key_links:
    - from: "src/components/review/ReviewConfirmStep.tsx"
      to: "src/lib/karute/draft.ts"
      via: "saveDraft call before rendering SaveKaruteFlow"
      pattern: "saveDraft"
    - from: "src/components/review/ReviewConfirmStep.tsx"
      to: "src/components/karute/SaveKaruteFlow.tsx"
      via: "renders SaveKaruteFlow component"
      pattern: "SaveKaruteFlow"
    - from: "src/app/(app)/karute/page.tsx"
      to: "karute_records table"
      via: "Supabase select for listing records"
      pattern: "from\\('karute_records'\\)"
---

<objective>
Wire the AI review screen (Phase 2) to the save flow (Plan 03), add a karute list page for browsing past records, and verify the end-to-end flow works: review → select customer → save → redirect to karute detail.

Purpose: Complete the integration between Phase 2 (AI pipeline) and Phase 4 (karute records), making the full recording-to-record flow functional (KRT-03). Add a karute list page so users can find and open past sessions.
Output: Review screen confirm step wired to SaveKaruteFlow, karute list page at /karute, end-to-end flow working.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-karute-records/04-CONTEXT.md
@.planning/phases/04-karute-records/04-RESEARCH.md
@.planning/phases/04-karute-records/04-01-SUMMARY.md
@.planning/phases/04-karute-records/04-02-SUMMARY.md
@.planning/phases/04-karute-records/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AI review screen confirm step to SaveKaruteFlow</name>
  <files>src/components/review/ReviewConfirmStep.tsx, src/app/(app)/review/page.tsx</files>
  <action>
Create or modify `src/components/review/ReviewConfirmStep.tsx`:
- This is the final step of the Phase 2 AI review screen, rendered after the user has confirmed/edited all extracted entries.
- On entering this step, call `saveDraft()` from `@/lib/karute/draft` to write the current review state (transcript, summary, confirmed entries array, staffId if available, duration) into sessionStorage.
- Render the `SaveKaruteFlow` component from `@/components/karute/SaveKaruteFlow`. Pass in the pre-fetched customer list if available from the parent, or let SaveKaruteFlow fetch its own.
- If the Phase 2 review page already has a confirm/save step component, integrate into it rather than creating a new file. Check for existing review page structure at `src/app/(app)/review/page.tsx` or similar paths and adapt accordingly.
- If no review page exists yet (Phase 2 not built), create a minimal placeholder at `src/app/(app)/review/page.tsx` that:
  1. Reads draft from sessionStorage (for testing the flow independently)
  2. Shows a "Review Complete" heading with the SaveKaruteFlow component below
  3. Includes a TODO comment: "Replace with full Phase 2 review UI when AI pipeline is built"

Map the Phase 2 entry format to the SaveKaruteInput entry format expected by saveKaruteRecord:
- category → category
- content/title → content
- sourceQuote/originalQuote → sourceQuote
- confidenceScore/confidence → confidenceScore

Per user decision: after confirming entries, the customer combobox appears inline (not a separate page). The save button triggers immediately with no confirmation dialog.
  </action>
  <verify>TypeScript compilation passes. The review confirm step renders SaveKaruteFlow. Draft data is written to sessionStorage before SaveKaruteFlow mounts.</verify>
  <done>The AI review screen's confirm step writes draft to sessionStorage and renders the SaveKaruteFlow component with customer selector and save button.</done>
</task>

<task type="auto">
  <name>Task 2: Create karute list page for browsing past records</name>
  <files>src/app/(app)/karute/page.tsx, src/components/karute/KaruteListItem.tsx</files>
  <action>
Create `src/app/(app)/karute/page.tsx`:
- Server component. Fetch all karute_records for the current user's business, ordered by created_at descending (most recent first).
- Use PostgREST nested select to include customer name: `karute_records (id, created_at, summary, customers (id, name), entries (id))` — entries included only for count.
- Render a page with heading "Karute Records" and a list of KaruteListItem components.
- If no records, show an empty state message ("No karute records yet").

Create `src/components/karute/KaruteListItem.tsx`:
- Server or client component (server is fine — no interactivity needed).
- Props: karute record with nested customer and entry count.
- Display: customer name, date (formatted), number of entries, first ~80 chars of summary as preview.
- Entire item is a link to `/karute/[id]`.
- Style: card-like row matching dark theme. Subtle hover state.

Add the /karute route to the sidebar navigation if a sidebar nav config exists (check for nav config in layout or a constants file). If no sidebar config exists, skip and note in summary.
  </action>
  <verify>
Visit /karute in the browser:
1. Page lists saved karute records with customer name, date, and entry count
2. Clicking a record navigates to /karute/[id] detail view
3. Empty state shows when no records exist
  </verify>
  <done>Karute list page at /karute shows all saved records. Each item links to the detail view. Users can browse and find past sessions.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Review confirm step writes draft and renders SaveKaruteFlow
- Full flow: review screen → select customer → save → redirect to /karute/[newId]
- /karute page lists all saved records with customer name and date
- Clicking a list item navigates to /karute/[id] detail view
- sessionStorage draft is cleared after successful save
</verification>

<success_criteria>
The end-to-end flow works: AI review screen confirm step → customer selection → save → redirect to karute detail view. Users can also browse past karute records from the /karute list page. All four Phase 4 requirements (KRT-01 through KRT-04) are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/04-karute-records/04-04-SUMMARY.md`
</output>
