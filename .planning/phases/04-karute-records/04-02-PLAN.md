---
phase: 04-karute-records
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(app)/karute/[id]/page.tsx
  - src/components/karute/KaruteDetailView.tsx
  - src/components/karute/EntryCard.tsx
  - src/components/karute/CategoryBadge.tsx
  - src/components/karute/ConfidenceDot.tsx
  - src/components/karute/AddEntryForm.tsx
  - src/components/karute/KaruteHeader.tsx
  - src/components/karute/TranscriptSection.tsx
autonomous: true

must_haves:
  truths:
    - "User can open /karute/[id] and see AI summary, transcript, and all categorized entries"
    - "Entries display with color-coded category pill badges and confidence dots (green/yellow/red)"
    - "Transcript is collapsed by default and expands on click"
    - "User can add a manual entry via the always-visible inline form at the top of entries"
    - "Manual entries show a 'Manual' badge and no confidence dot"
    - "Entry edit/delete buttons appear on hover"
  artifacts:
    - path: "src/app/(app)/karute/[id]/page.tsx"
      provides: "Karute detail server page with async params"
      min_lines: 15
    - path: "src/components/karute/KaruteDetailView.tsx"
      provides: "Two-column layout: entries left, summary+transcript right"
      min_lines: 40
    - path: "src/components/karute/EntryCard.tsx"
      provides: "Entry row with category badge, content, confidence dot, hover actions"
      min_lines: 30
    - path: "src/components/karute/CategoryBadge.tsx"
      provides: "Color-coded pill badge for entry categories"
      min_lines: 10
    - path: "src/components/karute/AddEntryForm.tsx"
      provides: "Inline always-visible form with category selector + content textarea"
      min_lines: 40
  key_links:
    - from: "src/app/(app)/karute/[id]/page.tsx"
      to: "src/lib/supabase/karute.ts"
      via: "getKaruteRecord call"
      pattern: "getKaruteRecord"
    - from: "src/components/karute/EntryCard.tsx"
      to: "src/components/karute/CategoryBadge.tsx"
      via: "CategoryBadge import"
      pattern: "CategoryBadge"
    - from: "src/components/karute/AddEntryForm.tsx"
      to: "src/actions/entries.ts"
      via: "addManualEntry Server Action call"
      pattern: "addManualEntry"
    - from: "src/components/karute/CategoryBadge.tsx"
      to: "src/lib/karute/categories.ts"
      via: "getCategoryConfig import"
      pattern: "getCategoryConfig"
---

<objective>
Build the karute detail view page and the inline add entry form. After this plan, users can view any karute record with its AI summary, transcript, categorized entries, and add manual entries.

Purpose: Deliver the primary read experience for karute records (KRT-01) and manual entry addition (KRT-02).
Output: Server-rendered karute detail page with two-column layout, entry cards with category badges and confidence dots, collapsible transcript, and inline add entry form.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-karute-records/04-CONTEXT.md
@.planning/phases/04-karute-records/04-RESEARCH.md
@.planning/phases/04-karute-records/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CategoryBadge, ConfidenceDot, and EntryCard components</name>
  <files>src/components/karute/CategoryBadge.tsx, src/components/karute/ConfidenceDot.tsx, src/components/karute/EntryCard.tsx</files>
  <action>
Create `src/components/karute/CategoryBadge.tsx`:
- Import `getCategoryConfig` from `@/lib/karute/categories` and shadcn `Badge` component.
- Render a Badge with `variant="outline"` and the category's color classes applied via className.
- Accept `category: string` prop. Use getCategoryConfig to get color + label, falling back to Other for unknown values.
- Pill style: the Badge component with the Tailwind color classes from ENTRY_CATEGORIES gives the colored pill/badge per user decision.

Create `src/components/karute/ConfidenceDot.tsx`:
- Per user decision: confidence shown as color-coded dots, NO numbers.
- Props: `score: number | null`, `isManual: boolean`.
- If isManual or score is null, render nothing.
- Render a small filled circle (8px or w-2 h-2 rounded-full) with color:
  - Green (bg-green-400) for score >= 0.85
  - Yellow/amber (bg-amber-400) for score >= 0.70
  - Red (bg-red-400) for score < 0.70
- Add a title/tooltip showing the percentage for accessibility (e.g., title="85% confidence").

Create `src/components/karute/EntryCard.tsx`:
- Client component ('use client') for hover state and expand behavior.
- Props: entry object (from Plan 01 types), karuteRecordId (string).
- Layout per user decision: compact list item with category badge, content text, and confidence dot on one line.
- Source quote: shown on expand (click to toggle). Display as indented italic text below the content when expanded. This is for AI entries with sourceQuote.
- Manual entries: show a small "Manual" badge (muted style, e.g., bg-muted text-muted-foreground) next to the category badge.
- Hover-reveal action buttons: edit and delete icons/buttons visible only on hover (use group/group-hover Tailwind pattern). Delete calls `deleteEntry` Server Action. Edit can be a TODO for now (or toggle inline editing of content).
- Use `useTransition` for pending state when deleting.
  </action>
  <verify>TypeScript compilation passes. Components render without errors in isolation.</verify>
  <done>CategoryBadge renders colored pill badges for all 8 categories. ConfidenceDot renders green/yellow/red dots with no numbers. EntryCard shows category badge + content + confidence dot on one line with expandable source quote and hover-reveal actions.</done>
</task>

<task type="auto">
  <name>Task 2: Create karute detail page with two-column layout, add entry form, and collapsible transcript</name>
  <files>src/app/(app)/karute/[id]/page.tsx, src/components/karute/KaruteDetailView.tsx, src/components/karute/KaruteHeader.tsx, src/components/karute/TranscriptSection.tsx, src/components/karute/AddEntryForm.tsx</files>
  <action>
Create `src/app/(app)/karute/[id]/page.tsx`:
- Server component. Use Next.js 16 async params pattern: `params: Promise<{ id: string }>`, await before use.
- Call `getKaruteRecord(id)` from `@/lib/supabase/karute`.
- If no record found, call `notFound()`.
- Render `<KaruteDetailView karute={data} />`.

Create `src/components/karute/KaruteHeader.tsx`:
- Per user decision: show customer name, staff name, date (formatted), session duration. No avatar/photo.
- Accept karute record data as props. Display in a header bar above the two-column layout.
- Use Intl.DateTimeFormat or date-fns for date formatting. Duration as "XX min" if available.

Create `src/components/karute/TranscriptSection.tsx`:
- Client component ('use client') for collapse/expand state.
- Per user decision: transcript collapsed by default, click to expand.
- Show a "Transcript" heading with a chevron toggle. When collapsed, show first ~2 lines as preview. When expanded, show full transcript text.
- Use local useState for open/closed state.

Create `src/components/karute/AddEntryForm.tsx`:
- Client component ('use client').
- Per user decision: inline always-visible form at the TOP of the entries list. Category-first: user picks a category badge, then types content in a textarea, clicks add.
- Layout: Row of 8 category badges (clickable to select, selected one is highlighted/filled), a textarea below for content, and an "Add" button.
- Fields: category (required) + content text (required) only -- minimal per user decision.
- On submit, call `addManualEntry` Server Action with { karuteRecordId, category, content }.
- Clear form after successful add. Show pending state on the Add button during submission.
- Use useTransition or useActionState for pending state.

Create `src/components/karute/KaruteDetailView.tsx`:
- Client component wrapper (needs state for add entry form interaction).
- Per user decision: two-column layout. Entries on the LEFT, summary + transcript on the RIGHT. Stacks vertically on mobile/tablet.
- Use CSS grid: `grid-cols-1 lg:grid-cols-2` with appropriate gap.
- Left column: AddEntryForm at top, then flat list of EntryCard components (not grouped by category per user decision).
- Right column: AI summary section (rendered as prose text), then TranscriptSection below.
- Pass karute data down to child components.
  </action>
  <verify>
Visit /karute/[valid-id] in the browser and confirm:
1. Two-column layout renders (entries left, summary+transcript right)
2. Category badges show correct colors
3. Confidence dots show (no numbers)
4. Transcript is collapsed by default
5. Add entry form is visible at top of entries list
6. Adding an entry works and appears in the list
  </verify>
  <done>Karute detail page renders at /karute/[id] with two-column layout, header with customer/staff/date, entries with colored category badges and confidence dots, collapsible transcript, AI summary, and inline add entry form. Manual entries show "Manual" badge. Edit/delete buttons appear on hover.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /karute/[id] page renders server-side with nested data
- Two-column layout: entries left, summary+transcript right
- All 8 category badge colors render correctly
- Confidence dots are colored circles (no numbers)
- Transcript collapsed by default, expandable
- Add entry form always visible at top of entries
- Manual entries have "Manual" badge, no confidence dot
- Hover on entry reveals edit/delete buttons
</verification>

<success_criteria>
A user can navigate to /karute/[id] and see the complete karute record: header with customer/staff/date, entries with color-coded category badges and confidence dots, AI summary, collapsible transcript, and can add manual entries via the inline form.
</success_criteria>

<output>
After completion, create `.planning/phases/04-karute-records/04-02-SUMMARY.md`
</output>
