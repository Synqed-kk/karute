---
phase: 04-karute-records
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/karute/SaveKaruteFlow.tsx
  - src/components/karute/CustomerCombobox.tsx
  - src/components/karute/QuickCreateCustomer.tsx
  - src/lib/karute/draft.ts
autonomous: true

must_haves:
  truths:
    - "After confirming entries on the AI review screen, user sees an inline searchable combobox to select a customer"
    - "User can create a new customer inline via '+ New customer' shortcut without leaving the flow"
    - "Clicking save immediately persists the karute record and redirects to /karute/[newId]"
    - "Draft data (transcript, summary, entries) is passed from Phase 2 via sessionStorage and cleaned up after save"
  artifacts:
    - path: "src/components/karute/SaveKaruteFlow.tsx"
      provides: "Save flow UI: customer selector + save button, reads draft from sessionStorage"
      min_lines: 50
    - path: "src/components/karute/CustomerCombobox.tsx"
      provides: "Searchable customer combobox with inline + New Customer option"
      min_lines: 40
    - path: "src/lib/karute/draft.ts"
      provides: "sessionStorage helpers: saveDraft, loadDraft, clearDraft"
      min_lines: 15
  key_links:
    - from: "src/components/karute/SaveKaruteFlow.tsx"
      to: "src/actions/karute.ts"
      via: "saveKaruteRecord Server Action call"
      pattern: "saveKaruteRecord"
    - from: "src/components/karute/SaveKaruteFlow.tsx"
      to: "src/lib/karute/draft.ts"
      via: "loadDraft to read sessionStorage data"
      pattern: "loadDraft"
    - from: "src/components/karute/CustomerCombobox.tsx"
      to: "customer data"
      via: "fetches customer list for search"
      pattern: "customers"
---

<objective>
Build the save flow that connects the AI review screen (Phase 2) to karute record persistence. After confirming entries, the user selects a customer, optionally creates one inline, and saves -- redirecting to the new karute detail view.

Purpose: Complete the end-to-end path from AI extraction to persisted karute record (KRT-03). This is the bridge between Phase 2 output and Phase 4 storage.
Output: SaveKaruteFlow component with customer combobox, quick-create customer, sessionStorage draft helpers, and save-to-redirect flow.
</objective>

<execution_context>
@/Users/anthonylee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonylee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-karute-records/04-CONTEXT.md
@.planning/phases/04-karute-records/04-RESEARCH.md
@.planning/phases/04-karute-records/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sessionStorage draft helpers</name>
  <files>src/lib/karute/draft.ts</files>
  <action>
Create `src/lib/karute/draft.ts` with helpers for the Phase 2 -> Phase 4 handoff:

- `KaruteDraft` type: { transcript: string, summary: string, entries: Array<{ category: string, content: string, sourceQuote?: string, confidenceScore: number }>, staffId?: string, duration?: number, savedAt: number }
- `saveDraft(draft: Omit<KaruteDraft, 'savedAt'>)`: Write to sessionStorage key 'karute_draft' with current timestamp. Guard with typeof window check.
- `loadDraft(): KaruteDraft | null`: Read from sessionStorage, parse JSON. Discard drafts older than 1 hour (per research recommendation). Return null if missing, expired, or parse fails.
- `clearDraft()`: Remove 'karute_draft' from sessionStorage.

All functions safe for SSR (check `typeof window !== 'undefined'`).
  </action>
  <verify>TypeScript compilation passes. Functions handle SSR environment gracefully.</verify>
  <done>Draft helpers exist for save/load/clear with 1-hour expiry and SSR safety.</done>
</task>

<task type="auto">
  <name>Task 2: Create customer combobox and save flow components</name>
  <files>src/components/karute/CustomerCombobox.tsx, src/components/karute/QuickCreateCustomer.tsx, src/components/karute/SaveKaruteFlow.tsx</files>
  <action>
Create `src/components/karute/CustomerCombobox.tsx`:
- Client component ('use client').
- Searchable combobox for customer selection. Use shadcn Popover + Command (cmdk) pattern for searchable dropdown, or a simple input+dropdown if Command is not available.
- Accept props: `customers: Array<{ id: string, name: string }>`, `selectedId: string | null`, `onSelect: (id: string) => void`, `onCreateNew: () => void`.
- Show list of customers filtered by search input.
- At the bottom of the list, show a "+ New customer" option that calls onCreateNew.
- Per user decision: inline searchable combobox (not a modal/dialog).

Create `src/components/karute/QuickCreateCustomer.tsx`:
- Client component for inline quick customer creation.
- Per user decision: "+ New customer" shortcut -- quick name entry, creates and selects in one step.
- Simple inline form: name input + create button. On submit, call a Server Action or API to create the customer, then call onCreated(newCustomer) to select it.
- Import the customer creation action from Phase 3's actions (e.g., `@/actions/customers`). If Phase 3 actions don't exist yet, create a minimal `createQuickCustomer` Server Action in `@/actions/customers.ts` that inserts a customer with just a name.

Create `src/components/karute/SaveKaruteFlow.tsx`:
- Client component ('use client').
- This component is placed on the AI review screen (Phase 2) after the user confirms entries.
- On mount, call `loadDraft()` to get the draft data. If no draft, show an error/empty state.
- UI flow per user decisions:
  1. Show the customer combobox (inline, not a separate page).
  2. User selects a customer (or creates new one inline).
  3. Show a "Save" button. Per user decision: save immediately on click, no confirmation dialog.
  4. On click, call `saveKaruteRecord()` with draft data + selected customerId.
  5. saveKaruteRecord redirects to /karute/[newId] on success.
  6. After save call initiated, call `clearDraft()` to clean up sessionStorage.
  7. Show loading/saving state on the button during the action.
- Accept `customers` prop (pre-fetched list) or fetch client-side.
- For staffId: use the draft's staffId if present, otherwise TODO fallback (will be wired in Phase 5).
- Handle save errors: show error message via toast or inline alert. Do NOT redirect on error.
  </action>
  <verify>
TypeScript compilation passes. Components render correctly:
1. CustomerCombobox shows searchable list with "+ New customer" option
2. QuickCreateCustomer creates a customer and selects it
3. SaveKaruteFlow loads draft, shows customer selector, saves on click, and redirects
  </verify>
  <done>Save flow works end-to-end: draft loads from sessionStorage, user selects/creates customer, save persists record and redirects to karute detail view, draft is cleared.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Draft helpers handle save/load/clear with expiry
- CustomerCombobox is searchable with "+ New customer" option
- SaveKaruteFlow reads draft from sessionStorage
- Save button triggers saveKaruteRecord and redirects
- sessionStorage is cleared after save
- No confirmation dialog on save (per user decision)
</verification>

<success_criteria>
The save flow connects Phase 2 output to Phase 4 persistence. A user can select a customer (or create one inline), click save, and be redirected to the new karute detail view. Draft data flows via sessionStorage and is cleaned up after save.
</success_criteria>

<output>
After completion, create `.planning/phases/04-karute-records/04-03-SUMMARY.md`
</output>
