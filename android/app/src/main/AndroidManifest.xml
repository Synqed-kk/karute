<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Code 8: microphone foreground service — keeps WebView audio capture
             alive when the screen locks (Android mutes mic input for non-visible
             apps otherwise; iOS side does this via UIBackgroundModes:audio).
             Started/stopped by MainActivity's AudioRecordingCallback, never
             exported. The type declaration is REQUIRED on targetSdk 34+. -->
        <service
            android:name=".RecordingForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Counseling-session recording (parity with iOS NSMicrophoneUsageDescription).
         Capacitor's BridgeWebChromeClient requests BOTH of these for a WebView
         audio-capture request and denies the mic if EITHER is undeclared —
         RECORD_AUDIO alone is not enough. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Code 8: both required for RecordingForegroundService — FOREGROUND_SERVICE
         since API 28, the MICROPHONE-typed variant since API 34. Normal
         (install-time) permissions, no user prompt. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
</manifest>
