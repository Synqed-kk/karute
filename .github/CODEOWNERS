# Audit enforcement surface — second reviewer required (contract §8).
# NOTE: merge-blocking needs the branch-protection "require review from
# Code Owners" toggle — Anthony's repo setting, not ours to flip.
/.github/CODEOWNERS @alee046
/src/lib/audit.ts @alee046
/src/lib/audit-web.ts @alee046
/src/lib/audit-policy.ts @alee046
# The facade emit primitive itself (Fable direct audit, 2026-07-28):
# handler.ts owns logFacadeAudit — it reads FACADE_AUDIT_MAP and is the code
# that actually emits for every facade route. Same class as the three files
# above; protecting the map while leaving the emitter open gates the policy
# and not the behaviour.
/src/lib/app-api/handler.ts @alee046
/scripts/audit/ @alee046
# The four CP gate suites by NAME, not an audit-* glob (blind round,
# 2026-07-28): the glob also swept in routine feature suites — the audit-log
# viewer suite is Wave V's hot surface, and owner-gating it would bottleneck
# the lane (same reasoning as the *audit* decline below). Emission EXISTENCE
# is owned by CP7's registry-reality walker + CP8's registry diff, both
# protected here; the unprotected per-action suites pin row SHAPE
# (severity/detail/target) and stay routine-editable deliberately.
/src/__tests__/integration/audit-actions-taxonomy.test.ts @alee046
/src/__tests__/integration/audit-coveredby.test.ts @alee046
/src/__tests__/integration/audit-sdk-write-sites.test.ts @alee046
/src/__tests__/integration/audit-writer-emission.test.ts @alee046
/src/__tests__/integration/facade-audit*.test.ts @alee046
# Only the helpers the protected suites execute through (blind round,
# 2026-07-28: the whole-dir glob owner-gated four generic mock helpers that
# ordinary feature tests edit routinely).
/src/__tests__/integration/helpers/audit-emission.ts @alee046
/src/__tests__/integration/helpers/ast-access.ts @alee046
/src/__tests__/integration/helpers/sdk-write-methods.ts @alee046
/src/__tests__/integration/helpers/src-files.ts @alee046
/src/__tests__/integration/helpers/audit-lines.ts @alee046
# jest plumbing the protected suites execute through (fresh-eyes fix round
# 2): jest.config.ts picks which suites run at all; setup/ runs before every
# suite and could globally mock the emitters. Both change rarely — low ping
# cost. (Deliberately NOT widened to every *audit*-named test: the audit-log
# UI suites are routine feature-work surface — Wave V edits them constantly —
# and owner-gating them would bottleneck the lane; accepted residual.)
/jest.config.ts @alee046
/src/__tests__/integration/setup/ @alee046
# tsconfig.json is the tsc equivalent of jest.config.ts (Fable direct audit,
# 2026-07-28): `npx tsc --noEmit` is what enforces FacadeEndpointKey totality,
# and CP8 leans on that totality to let plain skip rows come and go without a
# ledger line. An `exclude` entry covering src/lib/audit.ts would retire that
# gate silently, so it belongs with the other plumbing the gates execute
# through.
/tsconfig.json @alee046
/docs/audit-weakening-ledger.md @alee046
# Whole workflows dir, not just ci.yml (fresh-eyes fix round 2): a new
# workflow file could impersonate the CI check name for the future
# required-status-check gate.
/.github/workflows/ @alee046
# Business isolation guard (phone-safety lock 3) — the machine that fences
# Business PRs off phone-owned territory. Weakening the fence = weakening the
# phone guarantee, so the gate scripts + territory config + the import-
# isolation suite are owner-gated like the audit surface above. (The
# workflows dir is already owned; these are the pieces outside it.)
/scripts/business/ @alee046
/src/__tests__/integration/business-isolation.test.ts @alee046
# The release lever itself: business_workspace_grants lives here, and one
# INSERT opens Business for a tenant. Nothing gated a later migration adding
# grant rows (or loosening that table's RLS) until this line.
/supabase/migrations/ @alee046
